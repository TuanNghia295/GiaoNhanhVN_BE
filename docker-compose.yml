services:
  api-giao-nhanh:
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
    networks:
      - giao-nhanh-network

networks:
  giao-nhanh-network:
    driver: bridge
